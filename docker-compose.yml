version: "3.5"

services:
  bilingual-text-extractor:
    container_name: bilingual-text-extractor
    logging:
      options:
        max-size: "50m"
        max-file: "3"
    networks:
      - docker_net

networks:
  docker_net:
    name: docker_net
    driver: bridge
